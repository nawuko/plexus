name: Pi Assistant
run-name: ${{ github.event.issue.title || github.event.pull_request.title || 'Pi Assistant' }}

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  pi-agent:
    if: contains(github.event.comment.body, '/pi')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref || '' }}
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Configure git identity
        run: |
          git config --global user.email "pi-agent@plexus.dev"
          git config --global user.name "Pi Agent"

      - uses: mcowger/pi-coding-agent-action@refs/heads/v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          provider: openrouter
          model: ${{ vars.PLEXUS_MODEL }}
          token: ${{ secrets.OPENROUTER_API_KEY }}
          base_url: ${{ secrets.PLEXUS_URL }}
          thinking_level: medium
          append_prompt: >
            Communication protocol: Post a SINGLE initial comment using
            `create_issue_comment` that acknowledges the request and includes a
            GitHub markdown checklist outlining your plan, for example:

              - [ ] Analyze the request
              - [ ] Implement the changes
              - [ ] Validate with typecheck, format, and tests
              - [ ] Create a pull request

            Do NOT post a separate acknowledgment comment followed by a plan
            comment — combine them into one.

            CRITICAL: You MUST use `update_issue_comment` FREQUENTLY — update
            the progress comment IMMEDIATELY after completing ANY meaningful
            work or sub-step. Do not wait until the end to update it. Check
            off items as you go by changing `- [ ]` to `- [x]`. Add new
            sub-items under existing steps as they become relevant. Do NOT use
            strikethrough or other formatting — use only `- [ ]` / `- [x]`
            checkbox syntax.

            Update the comment:
              - Immediately after analyzing/understanding the request
              - After each file you modify
              - After each validation command finishes (typecheck, format, test)
              - When you encounter blockers or need clarification
              - When creating a PR
              - When completing the task

            The user should be able to refresh the page and see your current
            progress without waiting for completion.

            Code changes: If the request involves any code changes — or if you
            make any modifications to files during your work — you MUST create a
            branch, commit the changes, and open a pull request using the
            `create_pull_request` tool. Changes that are not committed and
            submitted as a PR will be lost when this CI run ends. Never leave
            modified files uncommitted.

            Git identity: The git user is already configured as "Pi Agent"
            <pi-agent@plexus.dev>. Do NOT run `git config` commands to set
            user.email or user.name — they are already set up. Just use
            `git add`, `git commit`, and `git push` directly.

            BEFORE pushing your branch or creating a PR, you MUST run these
            validation commands and ensure they pass:

              1. `bun run build:frontend` — Build the frontend first (required for typecheck)
              2. `bun run typecheck` — TypeScript type checking across all packages
              3. `bun run format` — Biome formatting (this will auto-fix issues)
              4. `bun run test` — Run the test suite

            If `bun run format` makes changes, stage them with `git add` before
            committing. Do NOT create a PR if these checks fail — fix the issues first.

            When you create a pull request, update your progress comment to
            clearly announce it — include the PR number and link so the requester
            can easily find and review it.
